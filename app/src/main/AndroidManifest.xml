<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.t.mylearningprojects">

    <uses-permission android:name="android.permission.USE_FINGERPRINT"/>
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".constrainlayout.ConstrainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".livedataDB.LiveDataActivity">
        </activity>
        <activity
                android:name=".test.TestAActivity"
                android:label="TestAActivity">
        </activity>
        <activity
                android:name=".test.TestBActivity"
                android:label="TestBActivity"/>
        <activity
                android:name=".test.TestCActivity"
                android:label="TestCActivity"/>
        <activity android:name=".fingerprint.FingerPrintActivity">

        </activity>
    </application>

</manifest>